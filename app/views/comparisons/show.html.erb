<div class="row">
  <div class="col-md-12 text-center">
    <h1>Which one is better?</h1>
	<h4><%= @study_name %></h4>
  </div>
</div>
<br>
<div class="row">
  <div class="col-md-1"></div>
  <div class="col-md-10">
    <div class="jumbotron">
	  <div class="row">
        <div class="col-md-6">
		  <% if @item1.link %>
	        <h3 class="text-center"><%= link_to @item1.name, @item1.link, {target: "_blank"} %></a></h3>
	      <% else %>
		    <h3 class="text-center"><%= @item1.name %></a></h3>
		  <% end %>
	      <br>
	      <%= @item1.description %>
	    </div>
	    <div class="col-md-6">
	      <% if @item1.link %>
	        <h3 class="text-center"><%= link_to @item2.name, @item2.link, {target: "_blank"} %></a></h3>
		  <% else %>
		    <h3 class="text-center"><%= @item2.name %></a></h3>
		  <% end %>
	      <br>
	      <%= @item2.description %>
	    </div>
	  </div>
	  <br>
	  <br>
	  <div class="row">
	    <div class="col-md-3"></div>
	    <div class="col-md-6 text-center">
	      <%= link_to raw("<button class=\"btn btn-success btn-lg\">&larr; This one!</button>"), two_way_comparison_path(@item1.id, 1, @item2.id, 2) %>
	      <%= link_to raw("<button class=\"btn btn-primary btn-lg\">Not sure.</button>"), two_way_comparison_path(@item1.id, 1, @item2.id, 1) %>
	      <%= link_to raw("<button class=\"btn btn-success btn-lg\">This one! &rarr;</button>"), two_way_comparison_path(@item1.id, 2, @item2.id, 1) %>
	    </div>
	    <div class="col-md-3"></div>
	  </div>
    </div>
  </div>
  <div class="col-md-1"></div>
</div>